import { Request, Response } from "express";

export function ApiScope(req: Request, res: Response) {
  return () => {
    try {
    
    } catch (error) {
      
    }
  }
}

export function ApiResult(result: any) {

}